{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "//buildCommand": "npm install (not ci) is required because Railpack mounts a build cache at /app/node_modules/.cache. `npm ci` wipes node_modules entirely as its first step, which tries to rmdir the mounted .cache and dies with EBUSY. `npm install` leaves node_modules in place so the mount survives. --no-audit shaves a few seconds off the build and silences noise.",
    "buildCommand": "npm install --no-audit && npm run build",
    "//": "watchPatterns are evaluated relative to the repo root. Limiting to HOA-RESIDENTS/** stops sibling-app changes from triggering a redeploy of this service.",
    "watchPatterns": ["HOA-RESIDENTS/**"]
  },
  "deploy": {
    "//": "Next.js standalone server (PWA). Same shape as HOA-ENTERPRISE.",
    "startCommand": "node .next/standalone/server.js",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
