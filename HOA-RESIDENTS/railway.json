{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "//buildCommand": "Two concerns: (1) `npm install` not `ci` because Railpack mounts /app/node_modules/.cache and ci's wipe EBUSYs the mount. (2) --include=dev because Railway sets NODE_ENV=production at the service level, which makes `npm install` skip devDependencies — without it autoprefixer/tailwindcss/typescript/postcss aren't on disk and next/font fails with 'Cannot find module autoprefixer'.",
    "buildCommand": "npm install --no-audit --include=dev && npm run build",
    "//": "watchPatterns are evaluated relative to the repo root. Limiting to HOA-RESIDENTS/** stops sibling-app changes from triggering a redeploy of this service.",
    "watchPatterns": ["HOA-RESIDENTS/**"]
  },
  "deploy": {
    "//": "Next.js standalone server (PWA). Same shape as HOA-ENTERPRISE. NEXT_TELEMETRY_DISABLED=1 is required for sleepApplication to work: Next.js telemetry is outbound traffic that would keep the service perpetually awake.",
    "startCommand": "NEXT_TELEMETRY_DISABLED=1 node .next/standalone/server.js",
    "//sleepApplication": "App Sleeping: sleep after ~10 min with no outbound traffic; the next inbound request wakes it (first request may 502 during cold start). Low-traffic service, so idle RAM drops toward zero. Health checks are inbound and do not keep it awake.",
    "sleepApplication": true,
    "healthcheckPath": "/login",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
